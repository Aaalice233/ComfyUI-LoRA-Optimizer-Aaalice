[project]
name = "comfyui-lora-optimizer"
description = "GPU-first LoRA merge optimizer with tiled execution and persistent caching"
version = "2.0.0"
license = { text = "GPL-3.0-only" }
dependencies = []

[project.urls]
Repository = "https://github.com/Aaalice2333/ComfyUI-LoRA-Optimizer-Aaalice"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"

[tool.comfy]
PublisherId = "ethanfel"
DisplayName = "LoRA Optimizer"
